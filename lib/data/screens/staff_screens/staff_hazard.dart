import "package:flutter/material.dart";

class StaffHazard extends StatelessWidget {
  const StaffHazard({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Staff Hazard Page"));
  }
}
