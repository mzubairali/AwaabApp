import 'package:flutter/material.dart';

class StaffEscalations extends StatelessWidget {
  const StaffEscalations({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Escalations"));
  }
}
