import 'package:flutter/material.dart';

class StaffCompliance extends StatelessWidget {
  const StaffCompliance({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Compliance and Reporting"));
  }
}
