import 'package:flutter/material.dart';

class StaffAppointments extends StatelessWidget {
  const StaffAppointments({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Appointments"));
  }
}
