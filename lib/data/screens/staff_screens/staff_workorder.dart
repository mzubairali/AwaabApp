import 'package:flutter/material.dart';

class StaffWorkorder extends StatelessWidget {
  const StaffWorkorder({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Work Order"));
  }
}
