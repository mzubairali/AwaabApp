import 'package:flutter/material.dart';

class StaffInvestigation extends StatelessWidget {
  const StaffInvestigation({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Investigation"));
  }
}
