import 'package:flutter/material.dart';

class TenNotifications extends StatelessWidget {
  const TenNotifications({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Notifications'));
  }
}
