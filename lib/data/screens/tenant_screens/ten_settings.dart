import 'package:flutter/material.dart';

class TenSettings extends StatelessWidget {
  const TenSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Settings'));
  }
}
