import 'package:flutter/material.dart';

class TenAppointments extends StatelessWidget {
  const TenAppointments({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Appointments'));
  }
}
