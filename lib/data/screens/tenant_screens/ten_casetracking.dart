import 'package:flutter/material.dart';

class TenCasetracking extends StatelessWidget {
  const TenCasetracking({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Case Tracking'));
  }
}
