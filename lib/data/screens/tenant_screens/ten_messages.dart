import 'package:flutter/material.dart';

class TenMessages extends StatelessWidget {
  const TenMessages({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Messages'));
  }
}
