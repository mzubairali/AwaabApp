import 'package:flutter/material.dart';

class TenDashboard extends StatelessWidget {
  const TenDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Tenant Dashboard'));
  }
}
