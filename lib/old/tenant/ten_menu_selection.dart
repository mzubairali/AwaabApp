//import 'package:cmsapp/data/exit_app.dart';
//import 'package:supabase_flutter/supabase_flutter.dart';

class TenMenuSelection {
  final int value;
  //final supabase = Supabase.instance.client;
  // constructor
  TenMenuSelection({required this.value});

  void menuCall() {
    //ExitApp();
    switch (value) {
      case 7: //exit app
        {
          // ExitApp();
        }
    }
  }
}
